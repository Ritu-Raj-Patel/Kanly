Now set up Next.js 14 with App Router and Tailwind CSS, and create the basic pages for the Kanboard application:

Home / Dashboard – list all boards

Board detail page – show columns (To Do / Doing / Done) with tasks

Create board page – simple form to create a new board

Focus only on UI shells with mock data:

No authentication logic

No API routes

No database calls

No drag-and-drop logic yet (just visual placeholders)

Use static mock data to represent boards, columns, and tasks so the layout and component structure are clear.

The goal is to establish:

Page routing structure

Reusable UI components

Basic responsive layout using Tailwind

Do not implement backend, state management, or real interactions yet — UI scaffolding only.